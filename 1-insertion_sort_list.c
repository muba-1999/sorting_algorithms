#include "sort.h"

/**
 * insertion_sort - sorts an array in ascending order
 * @list: list o be sorted
 */
void insertion_sort_list(listint_t **list)
{
	listint_t sorted = NULL;
	listint_t *cur = *list;

	while (cur != NULL)
	{
		listint *next = cur->next;

	}
}
